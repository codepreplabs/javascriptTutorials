function scope(){
	var local = 500; // its a local variable and is available only within the function.
	console.log(local);
}

var global = 600; // this is a global variable and can be used throughout the script